# CanLII API key (apply: https://www.canlii.org/en/api/)
CANLII_API=

# Optional: bearer token to secure the MCP HTTP endpoint
MCP_AUTH_TOKEN=

# HTTP server port (only used with --transport http)
PORT=3000
